Public Class ControlBox
    Inherits Panel

    Public Sub New()
        Me.Size = New Point(152, 50)
        Me.Padding = New Padding(0, 0, 0, 40)
        Me.Dock = DockStyle.Right
    End Sub

End Class
